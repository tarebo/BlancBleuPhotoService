<%
//var domain = 'http://pigeon-htravel.com';
var
    domain = '',
    baseTitle = '',
    accTw = ''
;
%><!doctype html>
<html lang="ja">
<head prefix="og: http://ogp.me/ns# fb: http://ogp.me/ns/fb#">
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">
    
    <meta name="viewport" content="width=device-width, initial-scale=1.0,maximum-scale=1, minimal-ui">
    
    <title><%= conf.title %><%= baseTitle %></title>
    <meta name="description" content="<%= meta.description %>">
    <meta name="keyword" content="<%= meta.keyword %>">
    <meta name="author" content="">

    <meta property="og:type" content="website">
    <meta property="og:url" content="<%= meta.url %>">
    <meta property="og:site_name" content="">
    <meta property="og:title" content="<%= conf.title %><%= baseTitle %>">
    <meta property="og:description" content="<%= meta.description %>">
    <meta property="og:image" content="<%= meta.og_image %>">
    
    <meta name="format-detection" content="telephone=no,address=no,email=no">
    <%
   
     if(page === 'redirect')
     {
    %>
    <meta http-equiv="refresh" content=".1;URL=<%= redirect %>">
    <%   
     }
    var l = filename.split('ejs/')[1].split('/').length;
    conf.rel = '';
    for(var i=0; i<l; i++) {
        conf.rel = '../' + conf.rel;
    }
    conf.name = filename.split('/')[filename.split('/').length - 1].replace('.ejs','');
    if (typeof conf.js === 'string') { conf.js = [conf.js]; }
    if (typeof conf.css === 'string') { conf.css = [conf.css]; }

    for(var j=0; j<conf.css.length; j++)
    {
    %>
    <link rel="stylesheet" href="<%= rel %>css/<%= conf.css[j] %>.css">
    <%
    }
    %>
</head>
<body><% include ./gtag.ejs %>
<div id="fb-root"></div>
<a id="top"></a>